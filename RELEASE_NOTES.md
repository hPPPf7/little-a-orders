小A點餐 v1.0.4｜自訂訂單收款金額（Android 9 以上）

- 待出餐訂單金額旁新增「編輯」，可自行輸入實際收款金額（0–999999 元整數）。
- 保留原價與原有品項，可隨時按「恢復原價」。取消編輯不會修改訂單。
- 歷史、付款方式小計、封存總額與備份均保留並採用調整後金額。
- 舊訂單維持原先金額，更新不會修改既有紀錄；已完成訂單不提供金額編輯。

包含先前更新：

- 右上角按鈕改為「檢查更新」，避免誤以為目前顯示的版本是待安裝的新版本。
- 目前版本仍顯示在檢查結果中；檢查、下載與安裝流程不變。
- 可直接從 v1.0.2 接收本次更新，覆蓋安裝會保留訂單。

包含 v1.0.2 的修正：

- 右上角新增版本號與「更新」按鈕，可立即檢查，不受自動檢查間隔限制。
- 顯示正在檢查、下載中、已是最新版及更新失敗的說明。
- 查詢失敗不再鎖住 6 小時；回到 App 時經過 1 分鐘即可自動重試，也可以隨時手動檢查。
- 修正 GitHub 工作流在相同 Release 已存在時再次發布而失敗的問題，保留既有發布檔案。
- 已以實際發布的 v1.0.0 APK 在 Android 模擬器验证可自動偵測並下載 v1.0.1。個別裝置失敗時，新版會提供可判斷的提示。
- 若舊版沒有自動提示，請直接下載此 APK 覆蓋安裝；不要先解除安裝，訂單會保留。

包含 v1.0.1 的安全邊距修正：

- 修正 Android 狀態列、瀏海與底部導覽列可能遮住操作區的問題；由外層容器保留安全範圍，網頁實際可用高度會同步調整。
- 處理螢幕旋轉、側邊挖孔與鍵盤占用的區域。
- 觸控網頁上下增加 8px 留白，並保留瀏覽器提供的安全區域；桌面滑鼠操作的版面不增加額外留白。
- 使用原簽章升級，保留既有訂單，不需解除安裝。

原有功能：

- 大字、自適應、無捲動條的分頁操作。
- 每份 6 顆、最多混搭 2 種，自動計價。
- 付款方式單選，完成付款與出餐才進歷史。
- 日期封存、JSON 備份、離線點餐。
- 啟動與回到 App 時自動檢查更新（每次啟動後最多每 6 小時一次）；新版 APK 自動下載並驗證套件名稱、版本與簽章，確認後交由 Android 安裝。更新保留原有訂單。

下載 **little-a-orders.apk** 安裝；Android 可能要求允許此來源安裝。更新仍需要 Android 系統確認，並非無人值守的靜默安裝。首次安裝沒有任何測試訂單。

`little-a-orders-web.zip` 為可自行架設的網頁版；`SHA256SUMS.txt` 為下載校驗碼。

訂單只保存在目前裝置，沒有雲端同步。請定期匯出備份；刪除 App 或清除 App 資料會刪除訂單。
