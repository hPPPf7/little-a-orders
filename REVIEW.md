# 發布前審查

範圍：網頁點餐、儲存、PWA、Android WebView、APK 更新與發布工作流。

已修正：
- 讀取本機資料時驗證完整訂單結構、數量、顆數、金額與 ID；資料毀損時保留原始內容並阻止寫入。
- 寫入前重新讀取最新資料，降低分頁資料過時造成的覆寫。此 App 仍設計為單一工作台，未提供多裝置同步或跨分頁交易鎖。
- 離線快取採同一版本的完整資源，更新版等待舊頁面關閉後生效。Android 使用 APK 內附資源，不使用 Service Worker，避免 APK 更新後仍載入舊網頁。
- 本機測試伺服器只提供明確列出的網站檔案，不會提供簽章、Git 或測試輸出。
- Android 只允許載入 APK 內附頁面，禁止外部頁面存取備份 bridge；關閉 WebView 檔案、內容與明文網路存取。
- Android 備份使用系統檔案選擇器。
- 更新限定本專案 GitHub Release 的 APK，下載後核對套件名稱、遞增版本和相同簽章，再交由 Android 確認安裝。
- 簽章不入庫；本機密碼使用 Windows DPAPI 加密，CI 使用 GitHub Secrets。
- 網頁及 APK 打包採白名單，不包含任何測試訂單、備份、簽章或測試畫面。

驗證：Node 計價／狀態／資料驗證測試；隔離瀏覽器的點餐、付款、封存及 6 種尺寸回歸；GitHub 工作流執行 Android unit tests、lint、簽署建置與模擬器內附網頁及重建 Activity 後訂單保存測試。僅在所有必要步驟成功後發布 Release。

限制：APK 靜默安裝取決於裝置管理權限，一般 Android 需確認安裝。資料清除／解除安裝會刪除本機訂單；請定期備份。未接入實際金流，付款按鈕為員工收款紀錄。
